<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="LightTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/colorLightBackground</item>
        <item name="cardViewStyle">@style/CardView.Light</item>
        <item name="errorTextColor">@color/warningDark</item>
        <item name="errorIconDrawable">@drawable/baseline_warning_24</item>
        <item name="android:screenOrientation">portrait</item>

    </style>

    <!-- Base application theme. -->
    <style name="DarkTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">@color/colorDarkBackground</item>
        <item name="cardViewStyle">@style/CardView.Dark</item>
        <item name="android:screenOrientation">portrait</item>

    </style>

</resources>